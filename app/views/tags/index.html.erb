<h1>All tags</h1>

<ul>
  <% @tag.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag) %> [<%= link_to "🗑️", tag_path(tag), method: :delete, data: {confirm: "Really delete the tag?"} %>]</li>
  <% end %>
</ul>
